<div class="contPrincipal">
    <div class="plantilla">
        <div class="image-carousel">
            <div class="carousel-track">
                <img src="../../Media/asesor.png" alt="Asesor 1" />
                <img src="../../Media/asesora.png" alt="Asesor 2" />
                <img src="../../Media/aula.jpg" alt="Asesor 3" />
            </div>
        </div>
        <div class="contenido">
            <div class="nombre">
                <h3>Quiénes somos?</h3>
            </div>
            <div class="info">
                <p> Somos una organización especializada en la enseñanza de matemáticas, física y química, desde nivel básico hasta nivel superior.  </p>
                <p> Nuestro equipo de trabajo se conforma por ingenieros y físicos. Los cuales aparte de ser especialistas en sus respectivas 
                    áreas de concentración, están altamente calificados para llevarte de la mano, siempre pensando en la necesidad de cada 
                    alumno y poder brindar un conocimiento con bases sólidas. </p>
                
                <p> Priorizamos la atención personaliza antes que la grupal, ya que, todos tenemos maneras y ritmos diferentes de aprender. 
                    Por ello, nos enfocamos en cada uno de nuestros alumnos de forma personal, así, el alumno pueda generar confianza y seguridad, 
                    tanto en la cuestión académica como en su día a día.  </p>
            </div>
            <div class="boton">
                <input type="button" value="Más sobre nosotros" >
            </div>

        </div>
    </div>
</div>
<style>
    .plantilla{
        width: 90%;
        display: grid;
        align-items: center;
        margin: 7rem;
        grid-template-columns: repeat(2, 1fr);
        gap:2rem;
        padding: 2rem;
        border-radius: 2rem;
        height: auto;
        background-color: var(--color-primario);
        
    }
    .image-carousel {
    width: 100%;
    height: auto;
    overflow: hidden;
    border-radius: 1rem;
}

.carousel-track {
    display: flex;
    width: 100%;
    animation: slide 9s infinite ease-in-out;
}

.carousel-track img {
    width: 100%;
    height: auto;
    flex-shrink: 0;
    object-fit: cover;
    border-radius: 1rem;
}

/* Animación: cambia cada 3s entre 3 imágenes */
@keyframes slide {
    0% { transform: translateX(0); }
    33% { transform: translateX(0); }

    36% { transform: translateX(-100%); }
    66% { transform: translateX(-100%); }

    69% { transform: translateX(-200%); }
    99% { transform: translateX(-200%); }

    100% { transform: translateX(0); }
}

    .contenido{
        display: flex;
        flex-direction: column; /* Elementos en columna */
        justify-content: center;
    }
    .nombre {
    text-align: left; /* Alinear el texto a la izquierda */
    font-size: 2rem;
    font-weight: bold;
    }

    .info {
        margin-top: 1rem;
    }

    .boton {
        margin-top: 1.5rem;
        display: flex;
        justify-content: center;
    }

    .boton input{
        background-color: var(--color-fondo);
        border: none;
        padding: .8rem 1.5rem;
        border-radius: 3rem;
        color: var(--color-boton);
        cursor: pointer;
        align-self: center;
    }
    .boton input:hover{
        background-color: var(--color-fondo);
        color:var(--color-boton);
        font-weight: bold;
    }

    @media (max-width: 768px)  {
        .plantilla{
            display: flex;
            flex-direction: column;
            margin: auto;
        }
    }
</style>